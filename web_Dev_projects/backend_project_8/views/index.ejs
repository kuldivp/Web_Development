<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body style="background-color: rgb(36, 35, 35);">
    <div class="main w-full min-h-screen bg-zinc-900">
        <div class="form p-10">
            <form action="/create" method="post">
                <input class="block w-full rounded-md outline-none px-5 py-2 bg-zinc-800 text-white" type="text"
                    placeholder="title goes here" name="title">
                <textarea class="block mt-2 w-full rounded-md outline-none px-2 py-2 bg-zinc-800 text-white resize-none"
                    placeholder="write your task details" name="details"></textarea>
                <input class="mt-2 bg-blue-500 rounded-md px-5 py-2 text-white cursor-pointer" type="submit"
                    value="create task">
            </form>
        </div>

        <div class="task flex gap-3 flex-wrap p-10">
            <% if (files.length > 0) { %>
                <% files.forEach(function(val) { %>
                    <div class="task w-72 px-3 py-4 bg-zinc-800">
                        <h3 class="text-white text-3xl tracking-tighter"><%= val.title %></h3>
                        <a class="text-blue-500 inline-block mt-3" href="<%= val.url %>">read more</a>
                    </div>
                <% }); %>
            <% } else { %>
                <h3 class="text-zinc-600">No tasks yet</h3>
            <% } %>
        </div>
    </div>
</body>

</html>
